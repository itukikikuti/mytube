services:
  backend:
    build:
      context: ./backend
    ports:
      - "8081:8080"
    environment:
      VIDEO_DIR: /videos
    volumes:
      - /mnt/nas/Videos:/videos:ro

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
